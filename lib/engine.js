const fontSizeFunc = require('./fontSize');
const lineHeightFunc = require('./lineHeight');
const defaultFunc = require('./default');


module.exports = {
    'font-size': fontSizeFunc,
    'line-height': lineHeightFunc,
    'default': defaultFunc
};
